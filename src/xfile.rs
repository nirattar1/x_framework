// a utility library for interacting with files.

pub struct XFile {
    m_filename: String
}

impl XFile {
    /**
     * read x bytes from file , into vector of chars.
     * @return bool if succeeded, false otherwise.
     */
    pub fn read(bytes: usize, vec: &mut Vec<char>) -> bool {

    }
}